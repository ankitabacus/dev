import { PipeTransform } from '@angular/core';
export declare class HighlightPipe implements PipeTransform {
    transform(text: string, search: string): string;
    EscapeMatch(match: string): string;
    Strip(str_in?: String): string;
}
